# 공개 공력 계수식 회수·적용성 감사 — ADR-0003 §4-2 ②

**산출물은 공력 모델 구현이 아니라 회수·적용성 감사다.** 비생산.
생산 코드는 변경하지 않았다.

## 0. 원문 고정

| | |
|---|---|
| 문서 | NASA/CR—2012–217475 |
| NTRS ID | 20130003336 |
| 발행 | 2012년 11월, Marshall Space Flight Center, 계약 NNM12AA41C |
| 저자 | G.L. Watts (Jacobs ESSSA / Qualis), J.W. McCarter (Jacobs ESSSA / DESE) |
| 분량 | 28쪽 |
| 배포 | **Unclassified-Unlimited**, Subject Category 02 |
| 크기 | 624,193 바이트 |
| **SHA-256** | `9e3b026b7684f9e118a1379cb228b8418728b140f5c106df3bb58b979a97d85d` |

원문 PDF 는 커밋하지 않는다(`.gitignore`). `./fetch.sh` 가 받고 해시를
검증한다.

**인용 규약**: 제목에 제한 도메인 용어가 있어 본문에서는 문서 번호로만
인용한다(ADR-0003 §0). 형상은 **원통 동체 + 고정 미익 + 노즈콘**이다.

## 1. 구성

| 절 | 쪽 | 내용 |
|---|---|---|
| INTRODUCTION | 1 | 기준량 정의, 적용 목적 |
| STATIC COEFFICIENT MODEL | 2–4 | M 프레임, 정적 계수 6개, 식 (1)–(3) |
| AERODYNAMIC DAMPING MODEL | 5–6 | 감쇠 미계수 3개, 식 (4)–(9) |
| AERODYNAMIC FORCES AND MOMENTS | 7–8 | 힘·모멘트 식 (10)–(18) |
| APPENDIX A | 9–11 | 감쇠 미계수의 수학적 등가 |
| APPENDIX B | 12–… | 교차류 원통의 피치 감쇠 모멘트 |

## 2. 좌표계 — 세 개다

| 프레임 | 원점 | 축 정의 |
|---|---|---|
| **M** | MRP (중심선 위) | X = 중심선 전방. **Y ∥ V⃗_R × î** — 즉 V⃗_R 이 롤 자세와 무관하게 항상 X-Z 평면에 있다 |
| **P** | MRP | 축이 B 프레임과 평행 (MRP frame) |
| **B** | 무게중심 | X 전방, Y 우현, Z 우수계 완성 |

M 프레임이 이 문서의 핵심 장치다. **큰 각도 변위(텀블링)에서도 V⃗_R 이
X-Z 평면에 남으므로** 계수가 롤 자세에 무관해진다.

## 3. 기준량

| 기호 | 정의 | 분류 |
|---|---|---|
| `S_ref` | **원통 단면적** | **형상에서 계산** |
| `D_ref` | **원통 직경** | **형상에서 계산** |

원문 서론이 명시한다. 별도 관례 추정이 필요 없다.

## 4. 회수한 식 전부

### 4-1. 각도·마하 (식 1–3, 3쪽)

```
α_tot = cos⁻¹( V_Rx / V_R )                     (1)   범위 0°~+180°, 음수 없음
φ_A   = tan⁻¹( V_Ry / V_Rz )                    (2)   범위 −180°~+180°
MACH  = V_R / V_sound                           (3)
```

`V_Rx, V_Ry, V_Rz` 는 **B 프레임의 V⃗_R 성분을 MRP 로 옮긴 것**.

### 4-2. 정적 계수 6개 (2–3쪽)

```
C_Am    축력      C_Ym    측력      C_Nm    법선력
C_lm    롤 모멘트  C_mm    피치 모멘트  C_yawm  요 모멘트
```

**부호 규약**: `C_Am`, `C_Nm` 은 각각 **X축·Z축의 반대 방향이 양**.
모멘트 셋은 **오른손 법칙**으로 양.

**축대칭 단순화 (원문 명시)**: 중심선 축대칭이면 M 프레임의 X-Z 평면이
V⃗_R 을 포함하는 대칭면이므로 **횡방향 계수 3개(`C_Ym`, `C_lm`, `C_yawm`)를
0 으로 둘 수 있다.** 남는 것은 축력·법선력·피치 모멘트 셋이다.

의존 변수: 일반적으로 (α_tot, φ_A, Mach). **축대칭이면 (α_tot, Mach) 뿐**
— φ_A 에 무관.

### 4-3. 감쇠 미계수 (식 4–6, 5쪽)

```
C_lpm   = ∂C_lm   / ∂( p_m · D_ref / (2 V_R) )      (4)
C_mqm   = ∂C_mm   / ∂( q_m · D_ref / (2 V_R) )      (5)
C_yawrm = ∂C_yawm / ∂( r_m · D_ref / (2 V_R) )      (6)
```

무차원화 인자는 **`D_ref / (2 V_R)`** — 항공기 관례의 `b/(2V)`, `c̄/(2V)`
와 달리 **세 축 모두 `D_ref`** 를 쓴다.

**적용 지점 규약** (5쪽, 중요):
- 감쇠 미계수는 **MRP 가 아니라 무게중심(에 가까운 곳)** 에서 개발한다
- 좌표계는 **M 프레임에 평행**하고 원점은 중심선 위 무게중심의 X 위치
- 개발 후 **MRP 에서 M 프레임으로 적용**한다 (순수 우력이므로 어느 점에나 적용 가능)
- 중심선에서의 작은 횡방향 무게중심 편차는 무시

각속도 `p_m, q_m, r_m` 은 **B 프레임 몸체 각속도를 중심선 둘레로
`−φ_A` 회전**시켜 얻는다.

### 4-4. 감쇠 모멘트 계수 (식 7–9, 6쪽)

계수와 각속도의 **선형 관계 가정**:

```
C_lmd   = ( p_m · D_ref / (2 V_R) ) · C_lpm         (7)
C_mmd   = ( q_m · D_ref / (2 V_R) ) · C_mqm         (8)
C_yawmd = ( r_m · D_ref / (2 V_R) ) · C_yawrm       (9)
```

### 4-5. M 프레임 힘·모멘트 (식 10–11, 7쪽)

```
F⃗_m = Q · S_ref · [ −C_Am , C_Ym , −C_Nm ]ᵀ                        (10)

M⃗_m = Q · S_ref · D_ref · [ C_lm + C_lmd ,
                             C_mm + C_mmd ,
                             C_yawm + C_yawmd ]ᵀ                    (11)
```

식 (10) 의 **1·3 성분에 음부호**가 붙는 것이 §4-2 의 부호 규약과 맞물린다.
모멘트는 `S_ref · D_ref` 로 차원화한다 — 세 축 공통.

### 4-6. M → P 변환 (식 12–13, 7쪽)

중심선 둘레로 공력 롤각 `φ_A` 회전:

```
        ⎡ 1     0        0    ⎤
R_pm =  ⎢ 0   cos φ_A  sin φ_A ⎥        F⃗_p = R_pm F⃗_m   (12)
        ⎣ 0  −sin φ_A  cos φ_A ⎦        M⃗_p = R_pm M⃗_m   (13)
```

### 4-7. P → B 변환 (식 14–18, 7–8쪽)

```
F⃗_b = F⃗_p                                                          (14)
M⃗_b = M⃗_p + MRP⃗_b × F⃗_p                                           (15)

M_Xb = M_Xp + MRP_Yb·F_Zp − MRP_Zb·F_Yp                             (16)
M_Yb = M_Yp + MRP_Zb·F_Xp − MRP_Xb·F_Zp                             (17)
M_Zb = M_Zp + MRP_Xb·F_Yp − MRP_Yb·F_Xp                             (18)
```

`MRP⃗_b` 는 **무게중심 기준 MRP 위치**(B 프레임).

## 5. 입력 분류 — 임의로 채우지 않는다

| 입력 | 분류 | 근거 |
|---|---|---|
| `S_ref` (원통 단면적) | **형상에서 계산** | 서론이 정의 |
| `D_ref` (원통 직경) | **형상에서 계산** | 서론이 정의 |
| `MRP⃗_b` (무게중심 기준 MRP) | **형상에서 계산** | 형상·질량 배치가 정해지면 유도 |
| `α_tot`, `φ_A`, `MACH` | **원문 제공** | 식 (1)–(3), 상태에서 계산 |
| `V_sound` | **외부 자료 필요** | 대기 모델. US Std 1976 등 공개 |
| `Q` (동압) | **외부 자료 필요** | 대기 밀도 × ½V² |
| **`C_Am(α_tot, Mach)`** | **외부 자료 필요** | **원문은 틀만 준다. 값은 없다** |
| **`C_Nm(α_tot, Mach)`** | **외부 자료 필요** | 〃 |
| **`C_mm(α_tot, Mach)`** | **외부 자료 필요** | 〃 |
| `C_Ym`, `C_lm`, `C_yawm` | **원문 제공 (축대칭 시 0)** | 대칭면 논증 |
| **`C_lpm`, `C_mqm`, `C_yawrm`** | **외부 자료 필요** | 정의(식 4–6)만 제공. 값 없음 |
| 미익 형상·`S_fin`, `C_fin` | **미상** | 부록 A 의 등가 논증에 등장하나 예제 형상 수치는 문서 목적 밖 |

### 5-1. 가장 중요한 공백

> **원문은 계수의 *구조·프레임·차원화·적용 순서* 를 준다. 계수 *값* 은
> 주지 않는다.**

`C_Am`, `C_Nm`, `C_mm` 을 α_tot ∈ [0°, 180°] × Mach 에 걸쳐 채우는 것은
별도 자료(풍동·CFD·준경험 추정)가 필요하다. 감쇠 미계수 셋도 마찬가지다.

**이것은 "회수 실패"가 아니다.** 회수한 것은 **틀 전체**이고, 그 틀이
요구하는 입력 목록이 위 표다. 자체 추정으로 계수를 만들더라도 **이 틀에
꽂아 넣으면 프레임·부호·차원화를 스스로 정하지 않아도 된다** — 그것이
이 회수의 값이다.

## 6. 저동압·저속·큰 받음각 적용성 — 별도 판정

| 영역 | 판정 | 근거 |
|---|---|---|
| **큰 받음각 (0°~180° 전 범위)** | **명시적으로 적용 가능** | 식 (1) 이 α_tot 범위를 0°~180° 로 정의. 문서 목적 자체가 텀블링 재진입 |
| **롤 자세 전 범위** | **적용 가능** | M 프레임이 V⃗_R 을 X-Z 평면에 고정. φ_A 범위 −180°~+180° |
| **저속·저동압** | **원문이 판정하지 않는다** | §6-1 |

### 6-1. 저동압에서 무엇이 무너지는가 — 형식적 검토

원문은 속도 하한을 논하지 않는다. 식에서 직접 보이는 것:

1. **식 (1) `α_tot = cos⁻¹(V_Rx / V_R)`** — `V_R → 0` 에서 정의되지 않는다.
   0으로 나누기.
2. **식 (4)–(9) 의 `D_ref / (2 V_R)`** — `V_R → 0` 에서 발산한다. 감쇠
   모멘트 계수가 무한대로 간다.
3. **식 (10)–(11) 의 `Q`** — `Q → 0` 이므로 힘·모멘트는 0 으로 간다.

2와 3이 상쇄되는지는 **곱을 보면 유한**하다:

```
M ∝ Q · S_ref · D_ref · (q_m D_ref / (2 V_R)) · C_mqm
  ∝ (½ ρ V_R²) · (q_m / V_R)
  ∝ ρ V_R q_m          →  V_R → 0 에서 0
```

즉 감쇠 모멘트 자체는 유한하고 0 으로 간다. **발산하는 것은 무차원 계수
`C_mmd` 이지 모멘트가 아니다.** 다만 구현이 계수 단계에서 클램프하면
그 클램프가 물리를 바꾼다.

**식 (1) 은 상쇄되지 않는다** — `V_R → 0` 에서 α_tot 자체가 정의되지
않으므로 정적 계수를 조회할 인자가 없다.

### 6-2. 판정

> **저동압·저속 영역의 적용성은 원문으로 판정할 수 없다.** 문서는 상승과
> 재진입을 대상으로 하며 속도 하한을 명시하지 않는다. 식 수준에서는
> 감쇠 모멘트가 유한하게 0 으로 가지만, **α_tot 정의가 V_R → 0 에서
> 무너지므로 정적 계수 경로에 하한이 필요하다.** 그 하한값은 원문이
> 주지 않는다.

초기 수직 구간의 시작 속도가 0 이 아님(ADR-0003 §2 경우 B)은 이 문제를
**완화하지만 제거하지는 않는다** — 하한을 얼마로 둘지는 여전히 결정 사항이다.

## 7. 중단 조건 대조

| 조건 | 해당 | 비고 |
|---|---|---|
| 원문 식이 **비공개 표·미제공 보정값**에 의존 | **부분 해당** | 계수 *값* 이 없다(§5-1). 다만 식 자체는 비공개 자료에 의존하지 않는다 |
| 적용 영역이 **초기 구간을 명백히 덮지 않음** | **해당 없음** | 큰 받음각은 명시적으로 덮는다. 저동압은 **판정 불가**이지 미적용이 아니다 |
| 좌표계·기준량을 **단일하게 복원 불가** | **해당 없음** | 세 프레임·변환·기준량 모두 명시적이고 유일하다 |

**결론: 중단하지 않는다.** 다만 산출물은 "완성된 공력 모델"이 아니라
**§4 의 틀 + §5 의 공백 목록**이다.

## 8. 다음 단계 (아직 하지 않음)

1. **독립 2회 전사 대조** — 이 문서는 1회차다. 2회차를 다른 경로로
   수행해 §4 의 식을 대조한 뒤에만 코드를 만든다
2. 비생산 **순수 계수 평가기** — 대조 통과 후
3. 자동 검사: 대칭성, 축·부호, 무차원 재폐쇄, α_tot = 0°/180° 한계,
   원문 예제
4. AIAA 공개 벤치마크 비교 — 위 통과 후

**기준 형상·적분기·합격선은 동결하지 않는다.**
